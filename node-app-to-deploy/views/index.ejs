<%- include('partials/header') %>

    <h3>Home page</h3>
    <nav>
        <ul class="pagination">
            <li class="page-item"><a class="page-link bg-dark text-light" href="?page=0">First</a></li>
            <li class="page-item"><a class="page-link bg-dark text-light"
                    href="?page=<%= locals.data.number > 0 ? locals.data.number -1 : locals.data.number  %>">Previous</a>
            </li>
            <li class="page-item disabled"><a class="page-link bg-dark text-light" href="#">
                    <%= locals.data.number +1 %>
                </a></li>
            <li class="page-item"><a class="page-link bg-dark text-light"
                    href="?page=<%= locals.data.number < locals.data.totalPages -1 ? locals.data.number +1 : locals.data.number  %>">Next</a>
            </li>
            <li class="page-item"><a class="page-link bg-dark text-light"
                    href="?page=<%= locals.data.totalPages -1  %>">Last</a></li>
        </ul>
    </nav>
    <table class="table table-dark table-striped">
        <thead>
            <tr>
                <th scope="col">ID</th>
                <th scope="col">BROJ LETA</th>
                <th scope="col">DESTINACIJA</th>
                <th scope="col">MODEL AVIONA</th>
                <th scope="col">VREME</th>
            </tr>
        </thead>
        <tbody>
            <% locals.data.content.forEach(flight=> {%>
                <tr>
                    <th scope="row">
                        <%= flight.id %>
                    </th>
                    <td>
                        <%= flight.flightNumber %>
                    </td>
                    <td>
                        <%= flight.destination %>
                    </td>
                    <td>
                        <%= flight.plane %>
                    </td>
                    <td>
                        <%= new Date(flight.scheduledAt).toLocaleString() %>
                    </td>
                </tr>
                <% }); %>
        </tbody>
    </table>
    <!-- Rendered: <%= JSON.stringify(locals.data) %> -->

    <%- include('partials/footer') %>